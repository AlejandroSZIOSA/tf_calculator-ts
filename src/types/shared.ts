export type Product = {
  id: string;
  name: string;
  weightPerSquareMeter: number;
};

export type User = {
  email: string;
  password: string;
};

export type Data = {
  token: string;
  userId: string;
};
